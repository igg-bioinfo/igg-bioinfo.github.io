source "https://rubygems.org"

# Use the github-pages gem to stay in sync with GitHub Pages' Jekyll version
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
end
